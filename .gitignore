node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
*.tsbuildinfo

# Cloud Run service exports — contain plaintext API keys pulled from the live
# service's env vars (`gcloud run services describe ... --format=export`).
# Never commit one of these.
service.yaml
service*.yaml
